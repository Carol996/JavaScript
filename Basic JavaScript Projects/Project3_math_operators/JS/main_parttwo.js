var J = 5;
J++;
document.write(J) ;

//shows as 64.25 on browser because there's no spacing between these functions//
var K = 5.25;
K--;
document.write(K);

window.alert(Math.random()); //returns a random number between 0 and 1//

window.alert(Math.random() * 100); //random number between __ and i.e. 100//

window.alert((Math.min (2, 3, 1) ));