//always make sure to put 'onclick' before the function to properly execute it//
onclick = function func ()    {
    var sent = "this is an example";
    sent += " of concatenation with plus-equal formula.";
document.getElementById("plusEq").innerHTML = sent;//no space before .innerHTML//
}

